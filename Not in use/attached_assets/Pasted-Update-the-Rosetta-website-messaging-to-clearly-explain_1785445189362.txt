Update the Rosetta website messaging to clearly explain its use of official hackathon sample data and the limitations of Demo Mode.

Important:
- Keep the existing visual design, layout, animations, colors, and five-agent workflow.
- Do not redesign the entire application.
- Do not remove existing functionality.
- Make focused copy and interface-label changes.
- Do not claim that Demo Mode modifies an external DataHub catalog.
- Confirm the exact official dataset name from the repository before displaying it. If the exact name cannot be verified, use "sample data provided through the official hackathon resources."
- Do not call the data real hospital or patient data.
- If the source does not explicitly describe the dataset as synthetic, do not call it synthetic.

1. HOMEPAGE HERO

Keep the current main headline:
"One metric. Two meanings. Silent chaos."

Add or replace the supporting description with:
"Rosetta finds conflicting metric definitions, traces their downstream impact through DataHub lineage, and prepares a human-approved remediation plan."

Make the primary button:
"Run Healthcare Demo"

Add this line directly beneath the button:
"No credentials required. Uses sample data provided through the official hackathon resources."

Add a small link next to or beneath that line:
"About this data"

2. ABOUT THIS DATA DIALOG

When the visitor selects "About this data," open a small modal or expandable panel.

Use this copy:
"This demonstration uses sample data provided through the official Build with DataHub Agent Hackathon resources. Rosetta analyzes the supplied data and metadata through a reproducible local evaluation workflow. It does not connect to a real healthcare organization or contain real patient information."

Only use the final sentence if it is verified by the source files and documentation. If it cannot be verified, replace it with:
"See the project documentation for the dataset source, license, and transformations."

Also display:
- Exact dataset name, if verified
- Official source link
- License
- Record count, if verified
- Whether Rosetta modified or augmented the source data
- Link to DATA_PROVENANCE.md

Do not invent any provenance information.

3. PERSISTENT DEMO MODE BADGE

On every demo workflow screen, add a visible but unobtrusive badge:
"DEMO MODE · Official hackathon sample data"

If "official hackathon sample data" is too strong based on the verified source, use:
"DEMO MODE · Hackathon resource dataset"

Add a tooltip:
"Rosetta analyzes a bundled local copy of the hackathon sample data. Approval generates a validated DataHub write plan but does not modify an external catalog."

Keep this badge visible during all five workflow stages.

4. APPROVAL SCREEN

In Demo Mode, replace:
"Approve & Write to DataHub"

With:
"Approve & Generate Write Plan"

Add this message directly above the button:
"Demo Mode: Approval will generate and validate the proposed DataHub operations. No external catalog will be modified."

Change any operation statuses before approval to:
"PROPOSED"

After approval, use:
"APPROVED"
"VALIDATED"
"NOT EXECUTED"

Never display "WRITTEN," "EXECUTED," or "UPDATED IN DATAHUB" in Demo Mode.

If the application supports a real connected mode, preserve the existing live write button only when a real DataHub connection has been verified:
"Approve & Write to DataHub"

5. FINAL RESULTS SCREEN

In Demo Mode, use this heading:
"Write plan approved and validated"

Use this supporting text:
"Rosetta prepared the operations required to reconcile this conflict in a connected DataHub catalog. No external operations were executed in Demo Mode."

Display a status summary:
- Human approval: Complete
- Write-plan validation: Passed
- External catalog modified: No
- Target platform: DataHub

Add buttons:
- "View Write Plan"
- "Download JSON"
- "Download Audit Report"

If "View Write Plan" does not currently exist, create a readable summary using the write-plan data already generated by the application. Do not fabricate an execution response.

6. SHORT EXPLANATION SECTION

Add a compact section near the bottom of the homepage titled:
"How Rosetta works"

Use these five steps:
1. Discover metric definitions in DataHub metadata
2. Detect incompatible meanings
3. Trace downstream impact through lineage
4. Propose a canonical definition
5. Generate a human-approved DataHub write plan

Add this paragraph beneath the steps:
"Rosetta is a semantic-consistency agent for DataHub. Using sample datasets provided through the official hackathon resources, Rosetta discovers where teams assign incompatible meanings to the same metric, traces each conflict through the metadata and lineage graph, measures downstream exposure, and proposes a canonical definition. The public demonstration requires no credentials. In Demo Mode, human approval generates a validated DataHub write plan without modifying an external catalog."

7. RESULTS TERMINOLOGY

Review the entire website and replace misleading Demo Mode language.

In Demo Mode:
- "Written to DataHub" becomes "Ready for DataHub"
- "Write completed" becomes "Write plan validated"
- "DataHub updated" becomes "Proposed DataHub update"
- "Assets linked" becomes "Assets identified for linking"
- "Terms deprecated" becomes "Terms flagged for deprecation"
- "Future agents inherit the truth" becomes "Connected agents can inherit the canonical definition after execution"

Do not change historical or explanatory text that is already accurate.

8. FINANCIAL LANGUAGE

Review the result cards for financial claims.

If the $28,478,287 amount is the sum of affected records, label it:
"$28.48M affected transaction value in the sample dataset"

Do not label it:
- cost avoided
- financial loss
- money saved

Unless the application has a documented calculation supporting that claim.

Add a "How calculated" tooltip with:
- source field
- affected-row filter
- aggregation method
- currency
- sample-data disclosure

If those details cannot be derived from the code, label the figure:
"Illustrative financial exposure"

9. ACCESSIBILITY AND MOBILE BEHAVIOR

Ensure:
- The Demo Mode badge is readable on mobile.
- Tooltips are accessible by keyboard and touch.
- The modal has a close button and Escape-key support.
- New text has sufficient color contrast.
- No text overlaps the graph or workflow controls.
- Buttons retain visible focus states.

10. TESTS

Add or update tests confirming:
- The homepage contains the no-credentials disclosure.
- The data-provenance link works.
- The Demo Mode badge appears on all five stages.
- Demo Mode uses "Approve & Generate Write Plan."
- Demo Mode cannot show a successful external write.
- The completion page says no external operations were executed.
- Live-mode language appears only after a verified connection.
- Existing exports and five-agent workflow still work.
- The page has no broken internal links.

11. FINAL VERIFICATION

Before finishing:
- Search the entire frontend for "write to DataHub," "written to DataHub," "executed," "cost avoided," and similar claims.
- Correct each phrase according to whether the application is in Demo Mode or connected mode.
- Verify the official dataset source before calling it official.
- Do not invent a dataset name, license, source URL, record count, or synthetic-data claim.
- Run all existing and new tests.
- Confirm the complete five-agent demo still works.
- Check desktop and mobile layouts.

At the end, provide:
1. A concise list of changed files
2. A summary of copy changes
3. Tests added or changed
4. Any claims that could not be verified
5. Screenshots of the homepage, approval screen, and final results screen
